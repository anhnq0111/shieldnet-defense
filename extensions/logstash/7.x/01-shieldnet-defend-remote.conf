# ShieldnetDefend - Logstash configuration file
## Remote ShieldnetDefend Manager - Filebeat input
input {
    beats {
        port => 5000
        codec => "json_lines"
    }
}

output {
    elasticsearch {
        hosts => ["localhost:9200"]
        index => "shieldnet-defend-alerts-4.x-%{+YYYY.MM.dd}"
    }
}